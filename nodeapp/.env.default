DATABASE_URL="postgresql://docker:docker@postgresql-db:5432/elogbook?schema=public"
NODE_PORT=3011
PORT=3011
USE_HTTP=1
HTTP_PORT=3010
MQTT_HOST=mqtt-broker
MQTT_PORT=1883
MQTT_USER=temptrack
MQTT_PASSWORD=changeme_mqtt_please_rotate

# Set in root .env / deploy.sh — do not glue onto MQTT_HOST line
JWT_SECRET=
